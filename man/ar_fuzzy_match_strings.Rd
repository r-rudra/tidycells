% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/after_render.R
\name{ar_fuzzy_match_strings}
\alias{ar_fuzzy_match_strings}
\title{Approximate matching of two character vectors}
\usage{
ar_fuzzy_match_strings(
  x,
  y,
  relationship = c("many_to_many", "one_to_one"),
  assignment_method = c("LSAP", "greedy"),
  case_sensitive = FALSE,
  retain_num = FALSE,
  add_acronyms = TRUE,
  wt_word = 0.55,
  wt_char = 0.15,
  wt_lcs = 0.3,
  lcs_min_chars = 3L,
  score_threshold = 0.1,
  emit_score_matrix = FALSE
)
}
\arguments{
\item{x}{\code{character} vector of strings to map \strong{from} (e.g. legacy labels).}

\item{y}{\code{character} vector of canonical target strings to map \strong{to}.}

\item{relationship}{\code{"many_to_many"} \emph{(default)} or \code{"one_to_one"}.
\itemize{
\item \code{"many_to_many"} — each \code{x[i]} independently picks its best \code{y[j]};
multiple \code{x} elements may map to the same \code{y} and vice-versa.
\code{assignment_method} is ignored in this mode (\code{"best"} is always used).
\item \code{"one_to_one"} — a strict bijection is enforced; each \code{x[i]} is paired
with at most one \code{y[j]} and vice-versa.  Use \code{assignment_method} to
choose between \code{"LSAP"} (globally optimal via \code{clue}) and \code{"greedy"}
(fast heuristic).
}}

\item{assignment_method}{\code{"LSAP"} \emph{(default for one_to_one)} or \code{"greedy"}.
Only consulted when \code{relationship = "one_to_one"}.
\itemize{
\item \code{"LSAP"} — solves the linear sum assignment problem via
\code{\link[clue:solve_LSAP]{clue::solve_LSAP()}}, guaranteeing a globally optimal bijection.
Requires the \strong{clue} package.
\item \code{"greedy"} — iterates pairs by descending score and accepts a pair
whenever both sides are still unmatched.  Faster but sub-optimal.
}}

\item{case_sensitive}{\code{logical(1)}. If \code{FALSE} \emph{(default)}, strings are
folded to lower-case before scoring.}

\item{retain_num}{\code{logical(1)}. If \code{FALSE} \emph{(default)}, digits are stripped
before scoring; if \code{TRUE}, digits are kept.}

\item{add_acronyms}{\code{logical(1)}. If \code{TRUE} \emph{(default)}, an acronym formed
from the first letter of each word is appended to any string that contains
two or more words (e.g. \code{"Private Final Consumption Expenditure"} →
\code{"Private Final Consumption Expenditure PFCE"}).  The augmented string is
then cleaned together with the original.}

\item{wt_word}{\code{numeric(1)}. Weight applied to the word-overlap Jaccard
score. Default \code{0.55}.}

\item{wt_char}{\code{numeric(1)}. Weight applied to the character-overlap Jaccard
score (computed only when the word or LCS score is non-zero). Default
\code{0.15}.}

\item{wt_lcs}{\code{numeric(1)}. Weight applied to the LCS ratio score. Default
\code{0.30}. \code{wt_word + wt_char + wt_lcs} must equal \code{1}.}

\item{lcs_min_chars}{\code{integer(1)}. Minimum length the longest common
substring must reach to contribute to the score. Default \code{3L}.}

\item{score_threshold}{\code{numeric(1)}. Pairs whose composite score falls below
this value are discarded (set to \code{NA} in the output). Default \code{0.10}.}

\item{emit_score_matrix}{\code{logical(1)}. If \code{TRUE}, the full \verb{m × n} numeric
score matrix is attached to the output as \code{attr(out, "score_matrix")}.
Default \code{FALSE}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\describe{
\item{\code{x}}{Original value from \code{x} (\code{NA} for unmatched \code{y} elements).}
\item{\code{y}}{Matched value from \code{y} (\code{NA} when no valid match found).}
\item{\code{score}}{Composite similarity score rounded to 4 decimal places
(\code{NA} when no match).}
}
All elements of \code{x} appear (one row each).  Elements of \code{y} that were not
matched to any \code{x} appear as additional rows with \code{x = NA} (full-join
semantics).  When \code{emit_score_matrix = TRUE} the full \verb{m × n} matrix is
available via \verb{attr(<result>, "score_matrix")}.
}
\description{
Computes a composite similarity score for every \verb{(x[i], y[j])} pair using
three complementary measures — word-overlap Jaccard, character-overlap
Jaccard, and longest-common-substring (LCS) ratio — then selects matches
according to the chosen relationship type and assignment method.
}
\details{
\strong{Scoring pipeline for each \verb{(x[i], y[j])} pair:}
\enumerate{
\item \emph{Augment} — optionally append first-letter acronym.
\item \emph{Clean} — strip punctuation (and optionally digits), squish whitespace,
optionally lower-case.
\item \emph{Word Jaccard} (\code{sw}) — \verb{|common words| / |union words|}.
\item \emph{LCS ratio} (\code{slcs}) — \code{nchar(LCS) / max(nchar(a), nchar(b))} on
space-stripped strings; zero if LCS < \code{lcs_min_chars}.
\item \emph{Char Jaccard} (\code{sc}) — \verb{|common chars| / |union chars|}; computed only
when \code{sw > 0} or \code{slcs > 0}.
\item \emph{Composite} — \code{wt_word * sw + wt_char * sc + wt_lcs * slcs}.
}
}
\examples{
\dontrun{
x_vec <- c(
  "Agriculture & allied activities",
  "Agriculture Forestry and Fishing",
  "Mining & Quarrying",
  "Manufacturing",
  "Electricity Gas & Water Supply",
  "Electricity Gas Water Supply & Other Utility Services",
  "Construction",
  "Trade hotelstransport & communication",
  "Trade hotels transport and Communication",
  "Trade Hotels Transport Communication and Services Related to Broadcasting",
  "Finance Insurance Real Estate & Business Services",
  "Finance InsuranceReal Estate & Business Services",
  "Financial Real Estate and Professional Services",
  "Community Social & Personal Services",
  "Public Administration Defence and Other Services",
  "Gross Value Added at Basic Prices"
)

y_vec <- c(
  "Agriculture, Livestock, Forestry and Fishing",
  "Mining and Quarrying",
  "Manufacturing",
  "Electricity, Gas, Water Supply & Other Utility Services",
  "Construction",
  "Trade, Hotels, Transport, Communication & Services Related to Broadcasting",
  "Financial, Real Estate & Professional Services",
  "Public Administration, Defence & Other Services",
  "Total Gross Value Added"
)

# Many-to-many (default) — multiple x can share the same y
ar_fuzzy_match_strings(x_vec, y_vec)

# One-to-one with globally optimal LSAP assignment
ar_fuzzy_match_strings(
  x_vec, y_vec,
  relationship      = "one_to_one",
  assignment_method = "LSAP"
)

# One-to-one with greedy assignment, emit score matrix
ar_fuzzy_match_strings(
  x_vec, y_vec,
  relationship      = "one_to_one",
  assignment_method = "greedy",
  emit_score_matrix = TRUE
)
}

}
\keyword{internal}
