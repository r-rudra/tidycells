% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/after_render.R
\name{rename_by_content}
\alias{rename_by_content}
\title{Rename data-frame columns by string / regex matching of their values}
\usage{
rename_by_content(
  d,
  name_map,
  name_map_regex = list(),
  include_cols = NULL,
  exclude_cols = NULL,
  assignment_method = c("LSAP", "greedy"),
  w_word = 0.5,
  w_phrase = 0.5,
  lambda = 0.5,
  min_score = 0.1,
  case_sensitive = FALSE,
  keep_details = FALSE,
  summary_msg = TRUE,
  warn = TRUE,
  reg_ex_match_bias = 0.1
)
}
\arguments{
\item{d}{A data frame whose columns are to be renamed.}

\item{name_map}{Named list of literal target strings; each name becomes a
candidate new column name. Scored with word-overlap + whole-phrase.}

\item{name_map_regex}{Named list of regex patterns, same shape as \code{name_map}.
Scored with pattern-match + matched-coverage only (no word-overlap term).
Defaults to an empty list.}

\item{include_cols}{Optional character vector of columns to restrict the
search to. \code{NULL} (default) considers every column of \code{d}.}

\item{exclude_cols}{Optional character vector of columns never to be mapped or
renamed (e.g. key columns). Applied after \code{include_cols}.}

\item{assignment_method}{\code{"LSAP"} (default, optimal via \code{clue::solve_LSAP}) or
\code{"greedy"}. If \code{clue} is unavailable, falls back to greedy with a warning.}

\item{w_word}{Weight in \verb{[0, 1]} on the word-overlap reward (fraction of a
target's words present in the entry). String nodes only. Default \code{0.5}.}

\item{w_phrase}{Weight in \verb{[0, 1]} on the whole-phrase reward (one entry is a
substring of a target, or vice versa). String nodes only; usually set so
\code{w_word + w_phrase == 1}. Default \code{0.5}.}

\item{lambda}{Penalty strength in \verb{[0, 1]} on unmatched content — leftover
words (string nodes) or uncovered characters (regex nodes). \code{0} disables,
\code{1} applies fully. Default \code{0.5}.}

\item{min_score}{Floor in \verb{[0, 1]} for a valid mapping. Winning pairs below it
are warned about and left unmapped. Default \code{0.1}.}

\item{case_sensitive}{Logical. If \code{FALSE} (default) matching is
case-insensitive; if \code{TRUE}, caseless matching is disabled.}

\item{keep_details}{Logical. If \code{TRUE} the result carries
\code{attr(., "score_matrix")} (nodes x columns) and \code{attr(., "mapping")}.
Default \code{FALSE}.}

\item{summary_msg}{Logical. If \code{TRUE} (default) emit a message listing which
\code{name_map} / \code{name_map_regex} nodes were not mapped and which candidate
columns kept their original names.}

\item{warn}{Logical. If \code{TRUE} (default) and \code{assignment_method = "greedy"},
warn when a step's best positive score is tied (resolved by first match).
Ignored for LSAP. \emph{(Warning for other cases also managed by same argument)}}

\item{reg_ex_match_bias}{Numeric. A tie-breaker bonus applied to regex match
scores based on the absolute number of matched characters. Default \code{0.1}.}
}
\value{
\code{d} with mapped columns renamed to their node names. Excluded,
non-included, invalid, and unmapped columns keep their original names.
}
\description{
Scores every valid candidate column of \code{d} against a set of \emph{nodes} (named
groups of literal target strings and/or regex patterns), assigns the
best-matching column to each node (optimally or greedily), and renames it to
the node's name.
}
\details{
Each (column, node) score lies in \verb{[0, 1]} and is computed over the column's
\strong{unique} values only. String nodes (\code{name_map}) combine a word-overlap
reward, a whole-phrase containment reward, and a leftover-word penalty. Regex
nodes (\code{name_map_regex}) use a pattern-match reward scaled by matched
character coverage (no word-overlap term). For both families the column score
is \verb{mean(score over matched uniques) * (1 - proportion non-matched)}.

Before scoring, columns that are entirely \code{NA} / empty and node targets that
are \code{NA} / zero-length are dropped; a node left with no targets is dropped
too. If no valid column or no valid node survives, \code{d} is returned unchanged
with a warning.

Assignment is \code{"LSAP"} (default, optimal — maximises the total score via
\code{clue::solve_LSAP}) or \code{"greedy"} (repeatedly take the global best pair).
A winning pair scoring below \code{min_score} is reported and left unmapped rather
than forcing a meaningless rename, so its node and column keep clear.

Performance: column unique-values and node targets/patterns are tokenised /
compiled once and cached, so the per-cell cost is just small set operations
and pattern matches — suitable for wide and/or long data frames.
}
\examples{
d <- tibble::tibble(
  id        = 1:5,                                              # key col
  region    = c("North", "South", "East", "West", "North"),     # key col
  category  = c("GVA at Basic Prices", "Agriculture", "Mining",
                "Manufacturing", "GVA at Basic Prices"),
  statement = c("Statement 1", "Statement 2", "Statement 1",
                "Statement 3", "Statement 2"),
  qtr       = c("2024-Q1", "2024-Q2", "2024-Q3", "2025-Q1", "2025-Q2"),
  isin      = c("INE001A01036", "INE002B02045", "INE003C03012",
                "INE004D04087", "INE005E05063"),
  note      = c("foo", "bar", "baz", "qux", "quux")             # matches nothing
)
name_map <- list(
  cat   = c("GVA at Basic Prices", "Agriculture, Forestry & Fishing"),
  info  = c("Statement 1", "Statement 2", "Statement 3"),
  extra = "Completely Unrelated Target Phrase"   # string node: will NOT map
)
name_map_regex <- list(
  period = "^\\\\d{4}-Q[1-4]$",    # -> qtr
  isin   = "^INE[A-Z0-9]{9}$",   # -> isin
  pin    = "^\\\\d{6}$"            # regex node: will NOT map
)
out <- rename_by_content(
  d, name_map, name_map_regex,
  exclude_cols = c("id", "region"),
  assignment_method = "LSAP",
  warn = FALSE
)
names(out)
}
