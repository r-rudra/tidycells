% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/after_render.R
\name{drop_constants}
\alias{drop_constants}
\title{Drop Constant Columns from a Data Frame}
\usage{
drop_constants(d, na.rm = TRUE)
}
\arguments{
\item{d}{A data frame or tibble.}

\item{na.rm}{\code{logical(1)}. If \code{TRUE} (the default), \code{NA} values are stripped
before counting unique values. If \code{FALSE}, \code{NA} is considered a distinct
value, meaning a column with one constant value plus \code{NA} will be kept.}
}
\value{
A data frame or tibble with all constant columns removed.
}
\description{
Evaluates all columns in a data frame and removes those that contain only a
single unique value. This is useful for cleaning up datasets by stripping out
zero-variance or uninformative variables.
}
\examples{
df <- tibble::tibble(
  id = 1:3,
  constant_num = c(5, 5, 5),
  constant_with_na = c("A", "A", NA),
  varying = c("X", "Y", "Z")
)

# By default (na.rm = TRUE), NA is ignored.
# Both 'constant_num' and 'constant_with_na' are dropped.
drop_constants(df)

# If na.rm = FALSE, NA counts as a distinct unique value.
# 'constant_with_na' is kept because it has two unique values: "A" and NA.
drop_constants(df, na.rm = FALSE)
}
