% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/after_render.R
\name{fuzzy_join}
\alias{fuzzy_join}
\title{Fuzzy Join}
\usage{
fuzzy_join(
  x,
  y,
  by = NULL,
  join_type = c("right", "left", "inner", "full"),
  retain_name_from = c("y", "x"),
  keep_score = FALSE,
  relationship = c("many_to_many", "one_to_one"),
  assignment_method = c("LSAP", "greedy"),
  case_sensitive = FALSE,
  retain_num = FALSE,
  add_acronyms = TRUE,
  wt_word = 0.55,
  wt_char = 0.15,
  wt_lcs = 0.3,
  lcs_min_chars = 3L,
  score_threshold = 0.1,
  emit_score_matrix = FALSE,
  retain_xy_cols = FALSE,
  ...
)
}
\arguments{
\item{x, y}{Data frames or tibbles to join.}

\item{by}{\code{NULL} (use the single common column name) or a length-1 string,
optionally named: \code{"col"} (same name in both) or
\code{c("x_col" = "y_col")} (different names).}

\item{join_type}{\code{character(1)}. The type of join to perform: \code{"right"},
\code{"left"}, \code{"inner"}, or \code{"full"}. Default is \code{"right"}.}

\item{retain_name_from}{\code{character(1)}. Which data frame's key column name
to retain in the final output. Defaults to \code{"y"}.}

\item{keep_score}{\code{logical(1)}. If \code{TRUE}, a score column is appended
showing the match confidence. Default \code{FALSE}.}

\item{relationship}{\code{character(1)}. Relationship for fuzzy matching:
\code{"many_to_many"} or \code{"one_to_one"}.}

\item{assignment_method}{\code{character(1)}. \code{"LSAP"} or \code{"greedy"}.}

\item{case_sensitive}{\code{logical(1)}. Fold strings to lower-case before scoring?}

\item{retain_num}{\code{logical(1)}. Retain digits during matching?}

\item{add_acronyms}{\code{logical(1)}. Append first-letter acronyms to strings?}

\item{wt_word, wt_char, wt_lcs}{\code{numeric(1)}. Weights for Jaccard and LCS
matching (must sum to 1).}

\item{lcs_min_chars}{\code{integer(1)}. Minimum length for longest common substring.}

\item{score_threshold}{\code{numeric(1)}. Minimum score to consider a valid match.}

\item{emit_score_matrix}{\code{logical(1)}. If \code{TRUE}, the full \verb{m × n} numeric
score matrix from the matching step is attached to the output as
\code{attr(out, "score_matrix")}.}

\item{...}{Further arguments forwarded to the underlying \code{dplyr} join
function (e.g., \code{suffix}, \code{na_matches}).}
}
\value{
A tibble in the style of \code{dplyr} join functions' output.
}
\description{
Performs a join where the key matching is approximate,
using fuzzy match strings to align string keys before joining.
}
\examples{
# 1. Your raw, messy data (e.g., from a quick survey or flash report)
df_raw <- tibble::tibble(
  sector_name = c("Agri & Fishing", "Mfg Sector", "Real Est. & Prof Services"),
  value = c(150, 420, 310)
)

# 2. Your clean, official master mapping
df_official <- tibble::tibble(
  canonical_sector = c(
    "Agriculture, Forestry and Fishing",
    "Manufacturing",
    "Financial, Real Estate & Professional Services",
    "Mining and Quarrying"
  ),
  weight = c(15.2, 18.1, 22.4, 2.5)
)

# 3. Perform the Fuzzy Join
# We use a left join to keep all our raw data, and we turn on `keep_score`
# so we can inspect how confident the algorithm was.
fuzzy_join(
  x = df_raw,
  y = df_official,
  by = c("sector_name" = "canonical_sector"),
  join_type = "left",
  keep_score = TRUE
)

}
